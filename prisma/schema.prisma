generator graphql {
  provider = "node ./dist/index.js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// try yours!

model User {
  email String  @unique
  id    Int     @default(autoincrement()) @id
  name  String?
}
